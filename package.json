{
  "name": "shrimp-microbiota-heatmap",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "preview": "node scripts/preview.mjs",
    "standardize": "node scripts/analysis.mjs standardize",
    "plot": "node scripts/analysis.mjs plot",
    "pretest": "npm run build",
    "test": "node scripts/check.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.13.0",
    "@playwright/test": "1.63.0",
    "esbuild": "0.28.2",
    "typescript": "7.0.2"
  }
}
